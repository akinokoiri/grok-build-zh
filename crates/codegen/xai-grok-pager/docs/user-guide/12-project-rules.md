# 项目规则（AGENTS.md）

在仓库中放置 `AGENTS.md`（或上游支持的规则文件），为代理提供**本项目专用**指令。

## 优先级

通常：更靠近当前目录的规则优先；用户全局规则与项目规则合并方式见上游说明。

## 建议写法

- 用清晰条目写构建/测试命令  
- 写清目录结构约定  
- 写清禁止事项（勿提交密钥、勿 force push 等）  

规则是给模型的上下文，不是强制沙箱；危险操作仍靠权限与 hooks。
