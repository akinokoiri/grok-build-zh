# 会话管理

## 基本操作

| 操作 | 命令/方式 |
|------|-----------|
| 新建 | `/new` |
| 恢复 | `/resume` |
| 分叉 | `/fork` |
| 回退 | `/rewind` |
| 压缩 | `/compact` |

## 存储

会话数据在 `~/.grok/sessions/` 下按工作目录编码分子目录。

不要手动编辑损坏的会话文件；优先用 TUI 导出/复制需要的内容。
